# Kindle connection photo

`kindle-device.png` is a transparent, display-sized derivative of the Kindle photo supplied by the user on 2026-09-06. The original upload is unchanged.

Prepared with the built-in image-editing tool, then downscaled to 192 pixels while preserving alpha. Used in the connection button and Kindle summary banner; no background tile is applied.

## Image-editing prompt

Use case: background-extraction

Asset type: miniature Kindle device photo for a web app connection banner.

Input image: edit target, user's black Kindle photo.

Primary request: Remove the entire checkerboard background and external ground shadow, leaving only this exact Kindle on a genuinely transparent alpha background. Keep the black device, its original proportions, front-on view, bezel, screen and printed Kindle logo unchanged. Center the complete device with only a narrow transparent margin. This must be a clean photographic cutout, not a redesigned icon. No white background, no checkerboard pixels, no halo, no extra objects or text.
